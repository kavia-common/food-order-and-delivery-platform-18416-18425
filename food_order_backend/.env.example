# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# JWT
JWT_SECRET=replace-with-a-strong-secret
JWT_EXPIRES_IN=1d

# Database (In this template we use in-memory store; if you plug a DB, use these)
DB_URL=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=
